Installation on Linux:

1. Make sure that Python 3.5+ is your default Python implementation.
2. In su command prompt use "pip install ." command from this directory
   (without quotes) to install scrapyard_helper script.
3. Copy .mozilla folder to your home directory, change path in the manifest
   if necessarry.