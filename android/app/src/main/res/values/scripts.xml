<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="injectCloudBookmarks">
        let bookmarks = #bookmarks#;
        bookmarks.shift();
        window.nodes = [...window.nodes, ...bookmarks];
        window.nodes.forEach(toJsTreeNode);
        window.tree.settings.core.data = window.nodes;
        window.tree.refresh(true);
        window.tree.open_node(\"-5\");
        $(\"#animation\").remove();
    </string>


</resources>
